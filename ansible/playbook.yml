---
# Base image playbook — applied by Packer during the Alpine image build.
# Installs cloud-init (latest) and base packages, then configures OCI datasource.

- name: Configure Alpine base image
  hosts: all
  gather_facts: false
  become: false # Packer connects as root

  roles:
    - base
