[defaults]
# Custom module library — rc_update and any future homelab-specific modules.
library            = library
roles_path         = roles
collections_path   = .collections
interpreter_python = /usr/bin/python3
# JSON stdout satisfies the "commands should produce JSON output" convention.
stdout_callback    = json
stderr_callback    = json

[ssh_connection]
pipelining = true
