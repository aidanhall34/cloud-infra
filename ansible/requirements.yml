---
# Ansible Galaxy collection requirements.
# Install with: cd ansible && uv run --active ansible-galaxy collection install -r requirements.yml -p .collections

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=2.1.0"
  - name: community.routeros
    version: ">=2.0.0"
